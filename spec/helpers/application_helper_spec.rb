require 'spec_helper'
describe ApplicationHelper do
  #describe "#render_status_counter" do
    #let(:user) {create :user}
    #let(:question) {create :question, user: user}
    #let(:answer) {create :answer, question: question, user: user}
    #it "displays link" do 
      ##expect(helper.render_status_answer question).to include user.username
      #expect(helper.render_status_counter).to include User.count.to_s
    #end
  #end
end
